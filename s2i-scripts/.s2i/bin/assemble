#!/bin/bash

echo "call builder image assemble"
/usr/libexec/s2i/assemble

rc=$?

if [ $rc -eq 0 ]; then
  echo "call builder image assemble successfully and copy index file"
  cp /tmp/src/index.html ./index2.html
else
  echo "call builder image assemble failed"
fi

exit $rc
